import SwiftUI

struct HomeView: View {
    var body: some View {
        VStack {
            Text("HOME PAGE")
        }
        .padding()
    }
}

struct HomeView_Previews: PreviewProvider {
    static var previews: some View {
        HomeView()
    }
}
